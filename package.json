{
  "name": "vk-board",
  "version": "1.0",
  "private": true,
  "scripts": {
    "heroku-prebuild": "yarn build:client && yarn build:server",
    "build:client": "cd client && yarn && yarn build",
    "build:server": "cd server && yarn && yarn build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/woophi/vk-board"
  },
  "engines": {
    "yarn": "1.22.4"
  }
}